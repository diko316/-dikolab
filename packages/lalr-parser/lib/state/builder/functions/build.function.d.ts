import type { StateMap } from '../../classes/state-map.class';
import type { Tokenizer } from '@dikolab/tokenizer';
export declare function build(root: string, map: StateMap, tokenizer: Tokenizer, definitions: unknown[], exclude?: unknown[]): boolean;
