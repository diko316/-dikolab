# platform-agnostic-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build platform-agnostic-lib` to build the library.

## Running unit tests

Run `nx test platform-agnostic-lib` to execute the unit tests via [Vitest](https://vitest.dev/).
